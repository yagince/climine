<% res.issues.each{|issue| -%>
( <%= Time.parse(issue.updated_on).strftime("%Y-%m-%d %H:%M:%S") %> ) #<%= issue.id %> [<%= issue.project.name %>][<%= issue.status.name %>] <%= issue.subject %>  
<% } -%>

TOTAL : <%= res.total_count %>
OFFSET: <%= res.offset %>
LIMIT : <%= res.limit %>